apiVersion: v1
kind: Service
metadata:
  name: flask-service-nodb
  namespace: mboukraa
spec:
  selector:
    app: flask-nodb
  type: NodePort
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30255
